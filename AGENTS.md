# AGENTS.md — Agent 自动加载入口

> 本文件是让 coding agent 自动发现规范文件的引导入口（AGENTS.md 会被主流 coding agent 自动读取）。

## 强制执行指令

**在执行本项目的任何任务之前，必须首先完整读取项目根目录的 `Agent.md` 文件**，并严格遵守其中定义的规则、文档约定与在线访问路径映射。

- `Agent.md` 是本项目的 Agent 规范文件（Agent Instructions），内容包含：强制执行规则、项目概述、文件与 GitHub Pages 访问路径对照表、学习主题与参考资料。
- 若 `Agent.md` 与 `AGENTS.md` 内容冲突，以 `Agent.md` 为准。
